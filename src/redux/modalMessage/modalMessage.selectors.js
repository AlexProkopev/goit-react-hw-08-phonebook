export const isModalMessageOpen = (state) => state.modalMessageStore.isModalOpen;
export const modalMessageData = (state) => state.modalMessageStore.dataModal;
export const isLoadingMessage = (state) => state.modalMessageStore.isLoading;